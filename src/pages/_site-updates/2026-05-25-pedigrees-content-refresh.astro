---
export const updateMeta = {
  date: '2026-05-25',
  title: 'Pedigrees workflow page refresh: comparison table + species coverage + expanded FAQ',
  tags: ['workflows', 'pedigrees', 'seo', 'content'],
};
---
<article class="border-l-4 border-[hsl(24,95%,53%)] pl-6">
  <h2 class="text-2xl font-bold text-gray-900 mb-1">May 25, 2026</h2>
  <p class="text-sm text-gray-500 mb-4">Refreshed <a href="/workflows/pedigrees" class="text-[hsl(24,95%,53%)] hover:underline">/workflows/pedigrees</a> with three targeted additions from an AirOps content review. The rest of the page was left as-is — the AirOps suggestion was ~85% paraphrase of existing copy, so we cherry-picked only the genuinely new material.</p>

  <div class="space-y-3 text-gray-700">
    <p><strong>What was added:</strong></p>
    <ul class="list-disc pl-6 space-y-1">
      <li>Species coverage sentence near the top of the page, explicitly listing all nine supported species (dogs, cats, horses, goats, sheep, cattle, alpacas, llamas, rabbits).</li>
      <li>New <em>"How BreederHQ compares to standalone pedigree tools"</em> section with an 8-row comparison table (paper pedigrees vs. standalone software vs. generic databases vs. BreederHQ) covering charts, COI, common ancestors, health-record linkage, hypothetical preview, genetics integration, portal sharing, multi-species support.</li>
      <li>Three new FAQ entries (added to both the rendered FAQ section and the schema.org FAQPage JSON-LD): species coverage, cloud-based vs. installation, and pedigree + genetics integration.</li>
    </ul>
    <p><strong>What was deliberately skipped:</strong></p>
    <ul class="list-disc pl-6 space-y-1">
      <li><em>"Last updated: July 2025"</em> stamp — stale and not used on workflow pages.</li>
      <li>Headline change — the AirOps headline matched the existing one verbatim.</li>
      <li>Wholesale rewrite of the existing problem/solution sections — would have been churn for paraphrase.</li>
      <li><em>"BreederHQ for Breed Clubs"</em> callout — link target unverified; deferred pending confirmation that the destination page exists.</li>
    </ul>
    <p class="text-sm text-gray-600">Page remains species-neutral per the shared/general workflow page rule. Comparison table widens the layout to <code>max-w-5xl</code> within that one section only; the rest of the page stays at <code>max-w-4xl</code>.</p>
  </div>
</article>
