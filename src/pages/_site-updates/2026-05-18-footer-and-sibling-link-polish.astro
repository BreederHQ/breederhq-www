---
export const updateMeta = {
  date: '2026-05-18',
  title: 'Footer + provider-page sibling link polish',
  tags: ['marketplace', 'ia', 'footer', 'farriers'],
};
---
<article class="border-l-4 border-[hsl(24,95%,53%)] pl-6">
  <h2 class="text-2xl font-bold text-gray-900 mb-1">May 18, 2026</h2>
  <p class="text-sm text-gray-500 mb-4">Final polish pass on the marketplace IA reinforcement. Two earlier 2026-05-18 fragments shipped the <a href="/services" class="text-[hsl(24,95%,53%)] hover:underline">/services</a> hub, the HeaderV2 Services dropdown, the 3-direction provider page pattern on <a href="/for-providers/farriers" class="text-[hsl(24,95%,53%)] hover:underline">/for-providers/farriers</a>, equine ecosystem reinforcement on <a href="/horses" class="text-[hsl(24,95%,53%)] hover:underline">/horses</a>, and farrier URL canonicalization. Two small gaps remained against the IA reinforcement spec.</p>

  <h3 class="text-lg font-semibold text-gray-900 mt-4 mb-2">Footer: direct category visibility</h3>
  <p class="text-gray-700 mb-3">
    The Footer Resources column had <code>/services</code> and <code>/for-providers</code> entries but no direct visibility to the two live marketplace categories. Added Farriers and Equine Boarding as their own footer lines so the marketplace ecosystem is reachable from every page without forcing a click through the Services hub.
  </p>

  <h3 class="text-lg font-semibold text-gray-900 mt-4 mb-2">Sibling provider links on /for-providers/farriers</h3>
  <p class="text-gray-700 mb-3">
    The third column of the 3-direction block (adjacent provider categories) previously emphasized veterinary, training, transport, and equine facilities. Reordered to lead with the categories closest to the farrier audience under the platform's actual taxonomy: equine services overview, training (dog trainers), pet boarding, veterinary, and equine facilities. Surfaces the real <code>/for-providers/training</code> and <code>/for-providers/boarding</code> pages rather than the spec-only <code>dog-trainers</code> / <code>pet-boarding</code> slugs that do not exist on disk.
  </p>

  <h3 class="text-lg font-semibold text-gray-900 mt-4 mb-2">Out of scope (explicitly not touched)</h3>
  <p class="text-gray-700 mb-3">
    No new pages. No new geo coverage. No homepage edits. No trust or verification expansion. No mega-nav restructuring. The marketplace IA reinforcement scope (R1, R2, R3, R6, R7, R8 Option A) is now complete across the three 2026-05-18 fragments.
  </p>
</article>
