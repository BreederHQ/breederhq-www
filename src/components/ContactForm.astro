---
/**
 * ContactForm Component
 * Reusable contact form with validation, formatting, tracking, and submission to API.
 */

interface InterestOption {
  value: string;
  label: string;
  description?: string;
}

interface Props {
  formId?: string;
  submitText?: string;
  source?: string;
  includePhone?: boolean;
  includeCompany?: boolean;
  includeMessage?: boolean;
  includeInterest?: boolean;
  interestLabel?: string;
  interestOptions?: InterestOption[];
  interestRequired?: boolean;
  interestMulti?: boolean;
  messageLabel?: string;
  messagePlaceholder?: string;
  phoneLabel?: string;
  phonePlaceholder?: string;
  companyLabel?: string;
}

const {
  formId = 'contact-form',
  submitText = 'Submit',
  source = 'contact_form',
  includePhone = true,
  includeCompany = true,
  includeMessage = true,
  includeInterest = false,
  interestLabel = "I'm interested in...",
  interestOptions = [],
  interestRequired = false,
  interestMulti = false,
  messageLabel = 'Message',
  messagePlaceholder = 'Tell us about your breeding program...',
  phoneLabel = 'Phone',
  phonePlaceholder = '(555) 123-4567',
  companyLabel = 'Company / Breeding Program',
} = Astro.props;

const inputBaseClass =
  'w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary focus:border-transparent';
const errorTextClass = 'mt-1 text-xs text-red-600 hidden';

const interestInputType = interestMulti ? 'checkbox' : 'radio';
const interestErrorMsg = interestMulti ? 'Please pick at least one.' : 'Please pick one.';
const interestHelperHint = interestMulti ? 'Pick all that apply.' : null;
---

<div class="contact-form-container">
  <form id={formId} class="space-y-4" data-source={source} novalidate>
    <!-- Name Field -->
    <div>
      <label for={`${formId}-name`} class="block text-sm font-medium text-gray-700 mb-1">
        Name <span class="text-red-500" aria-hidden="true">*</span>
      </label>
      <input
        type="text"
        id={`${formId}-name`}
        name="name"
        required
        aria-required="true"
        autocomplete="name"
        autocapitalize="words"
        maxlength="100"
        class={inputBaseClass}
        placeholder="Jane Smith"
        data-validate="name"
      />
      <p class={errorTextClass} data-error-for="name">Please enter your name.</p>
    </div>

    <!-- Email Field -->
    <div>
      <label for={`${formId}-email`} class="block text-sm font-medium text-gray-700 mb-1">
        Email <span class="text-red-500" aria-hidden="true">*</span>
      </label>
      <input
        type="email"
        id={`${formId}-email`}
        name="email"
        required
        aria-required="true"
        autocomplete="email"
        autocapitalize="none"
        inputmode="email"
        spellcheck="false"
        maxlength="254"
        class={inputBaseClass}
        placeholder="jane@example.com"
        data-validate="email"
      />
      <p class={errorTextClass} data-error-for="email">Please enter a valid email address.</p>
    </div>

    <!-- Phone Field (optional) -->
    {includePhone && (
      <div>
        <label for={`${formId}-phone`} class="block text-sm font-medium text-gray-700 mb-1">
          {phoneLabel}
        </label>
        <input
          type="tel"
          id={`${formId}-phone`}
          name="phone"
          autocomplete="tel"
          inputmode="tel"
          maxlength="25"
          class={inputBaseClass}
          placeholder={phonePlaceholder}
          data-validate="phone"
        />
        <p class="mt-1 text-xs text-gray-500" data-hint-for="phone">
          We default to US format. Start with <code class="font-mono">+</code> and your country code for international (e.g. <code class="font-mono">+44</code>, <code class="font-mono">+91</code>).
        </p>
        <p class={errorTextClass} data-error-for="phone">That doesn't look like a valid phone number. For international, start with <code class="font-mono">+</code> and the country code.</p>
      </div>
    )}

    <!-- Company Field (optional) -->
    {includeCompany && (
      <div>
        <label for={`${formId}-company`} class="block text-sm font-medium text-gray-700 mb-1">
          {companyLabel}
        </label>
        <input
          type="text"
          id={`${formId}-company`}
          name="company"
          autocomplete="organization"
          maxlength="120"
          class={inputBaseClass}
          placeholder="Acme Kennels"
        />
      </div>
    )}

    <!-- Interest Group (optional, single or multi-select) -->
    {includeInterest && interestOptions.length > 0 && (
      <fieldset
        data-validate="interest"
        data-required={interestRequired ? 'true' : 'false'}
        data-multi={interestMulti ? 'true' : 'false'}
      >
        <legend class="block text-sm font-medium text-gray-700 mb-1">
          {interestLabel} {interestRequired && (<span class="text-red-500" aria-hidden="true">*</span>)}
        </legend>
        {interestHelperHint && (
          <p class="text-xs text-gray-500 mb-2">{interestHelperHint}</p>
        )}
        <div class="space-y-2">
          {interestOptions.map((opt) => (
            <label
              class="flex items-start gap-3 p-3 border border-gray-200 rounded-lg cursor-pointer hover:border-primary hover:bg-primary/5 transition has-[:checked]:border-primary has-[:checked]:bg-primary/5"
            >
              <input
                type={interestInputType}
                name="interest"
                value={opt.value}
                class={
                  interestMulti
                    ? 'mt-1 h-4 w-4 rounded text-primary border-gray-300 focus:ring-primary'
                    : 'mt-1 h-4 w-4 text-primary border-gray-300 focus:ring-primary'
                }
              />
              <span class="flex-1">
                <span class="block font-medium text-gray-900 text-sm">{opt.label}</span>
                {opt.description && (
                  <span class="block text-xs text-gray-500 mt-0.5">{opt.description}</span>
                )}
              </span>
            </label>
          ))}
        </div>
        <p class={errorTextClass} data-error-for="interest">{interestErrorMsg}</p>
      </fieldset>
    )}

    <!-- Message Field (optional) -->
    {includeMessage && (
      <div>
        <label for={`${formId}-message`} class="block text-sm font-medium text-gray-700 mb-1">
          {messageLabel}
        </label>
        <textarea
          id={`${formId}-message`}
          name="message"
          rows="4"
          maxlength="2000"
          class={inputBaseClass}
          placeholder={messagePlaceholder}
        ></textarea>
      </div>
    )}

    <!-- Submit Button -->
    <div>
      <button
        type="submit"
        class="w-full bg-primary text-white px-6 py-3 rounded-lg font-semibold hover:opacity-90 transition-opacity disabled:opacity-50 disabled:cursor-not-allowed"
      >
        {submitText}
      </button>
    </div>

    <!-- Status Messages -->
    <div id={`${formId}-status`} class="hidden mt-4">
      <div class="success-message hidden p-4 bg-green-50 border border-green-200 rounded-lg">
        <p class="text-green-800">
          ✅ Thank you! We'll be in touch soon.
        </p>
      </div>
      <div class="error-message hidden p-4 bg-red-50 border border-red-200 rounded-lg">
        <p class="text-red-800">
          ❌ Something went wrong. Please check the fields above and try again.
        </p>
      </div>
    </div>
  </form>
</div>

<script>
  import { trackFormStart, trackFormSubmit, getUTMParams } from '../lib/tracking';
  import {
    AsYouType,
    parsePhoneNumberFromString,
    isValidPhoneNumber,
    type CountryCode,
  } from 'libphonenumber-js';

  const DEFAULT_COUNTRY: CountryCode = 'US';

  // -- Formatters --------------------------------------------------------------

  function titleCaseName(raw: string): string {
    return raw
      .trim()
      .replace(/\s+/g, ' ')
      .split(' ')
      .map((part) =>
        part
          .split('-')
          .map((seg) => (seg ? seg.charAt(0).toUpperCase() + seg.slice(1).toLowerCase() : seg))
          .join('-')
      )
      .join(' ');
  }

  /**
   * Live format phone number using libphonenumber-js AsYouType.
   * - No leading "+": treat as US number, format as the user types.
   * - Leading "+": let the lib detect the country from the dial code and format
   *   per that country's national convention.
   */
  function formatPhone(raw: string): string {
    if (!raw) return '';
    const trimmed = raw.replace(/^\s+/, '');
    if (!trimmed) return '';

    if (trimmed.startsWith('+')) {
      // International — AsYouType with no default country reads from the dial code.
      const formatter = new AsYouType();
      return formatter.input(trimmed);
    }
    // No "+" — assume US/Canada (NANP). User can switch by typing "+" first.
    const formatter = new AsYouType(DEFAULT_COUNTRY);
    return formatter.input(trimmed);
  }

  function isValidEmail(email: string): boolean {
    return /^[^\s@]+@[^\s@.]+\.[^\s@]{2,}$/.test(email);
  }

  function isValidName(name: string): boolean {
    const trimmed = name.trim();
    if (trimmed.length < 2) return false;
    return /\p{L}/u.test(trimmed);
  }

  function validatePhoneValue(value: string): { ok: boolean; e164: string } {
    if (!value.trim()) return { ok: true, e164: '' };
    const parsed = parsePhoneNumberFromString(
      value,
      value.trim().startsWith('+') ? undefined : DEFAULT_COUNTRY
    );
    if (!parsed) return { ok: false, e164: '' };
    const ok = parsed.isValid();
    return { ok, e164: ok ? parsed.number : '' };
  }

  // -- Per-form wiring ---------------------------------------------------------

  document.querySelectorAll('form[id$="-form"]').forEach((form) => {
    if (!(form instanceof HTMLFormElement)) return;

    const formId = form.id;
    const source = form.dataset.source || 'contact_form';
    let formStarted = false;

    const nameInput = form.querySelector<HTMLInputElement>('input[data-validate="name"]');
    const emailInput = form.querySelector<HTMLInputElement>('input[data-validate="email"]');
    const phoneInput = form.querySelector<HTMLInputElement>('input[data-validate="phone"]');
    const interestFieldset = form.querySelector<HTMLFieldSetElement>('fieldset[data-validate="interest"]');

    const fieldTouched: Record<string, boolean> = {};

    function showFieldError(field: string, show: boolean) {
      const errorEl = form.querySelector<HTMLElement>(`[data-error-for="${field}"]`);
      if (!errorEl) return;
      errorEl.classList.toggle('hidden', !show);

      if (field !== 'interest') {
        const input = form.querySelector<HTMLElement>(`[data-validate="${field}"]`);
        if (input) {
          input.classList.toggle('border-red-500', show);
          input.classList.toggle('focus:ring-red-500', show);
          input.classList.toggle('border-gray-300', !show);
        }
      }
    }

    function validateName(): boolean {
      if (!nameInput) return true;
      const ok = isValidName(nameInput.value);
      if (fieldTouched.name) showFieldError('name', !ok);
      return ok;
    }

    function validateEmail(): boolean {
      if (!emailInput) return true;
      const value = emailInput.value.trim();
      const ok = value.length > 0 && isValidEmail(value);
      if (fieldTouched.email) showFieldError('email', !ok);
      return ok;
    }

    function validatePhone(): boolean {
      if (!phoneInput) return true;
      const { ok } = validatePhoneValue(phoneInput.value);
      if (fieldTouched.phone) showFieldError('phone', !ok);
      return ok;
    }

    function getCheckedInterests(): string[] {
      if (!interestFieldset) return [];
      return Array.from(
        interestFieldset.querySelectorAll<HTMLInputElement>('input[name="interest"]:checked')
      ).map((el) => el.value);
    }

    function validateInterest(): boolean {
      if (!interestFieldset) return true;
      const required = interestFieldset.dataset.required === 'true';
      if (!required) return true;
      const ok = getCheckedInterests().length > 0;
      if (fieldTouched.interest) showFieldError('interest', !ok);
      return ok;
    }

    form.addEventListener('input', () => {
      if (!formStarted) {
        formStarted = true;
        trackFormStart(formId);
      }
    }, { once: true });

    // Name: live trim + Title Case on blur
    if (nameInput) {
      nameInput.addEventListener('blur', () => {
        fieldTouched.name = true;
        const formatted = titleCaseName(nameInput.value);
        if (formatted !== nameInput.value) nameInput.value = formatted;
        validateName();
      });
      nameInput.addEventListener('input', () => {
        if (fieldTouched.name) validateName();
      });
    }

    // Email: lowercase + trim on blur
    if (emailInput) {
      emailInput.addEventListener('blur', () => {
        fieldTouched.email = true;
        const cleaned = emailInput.value.trim().toLowerCase();
        if (cleaned !== emailInput.value) emailInput.value = cleaned;
        validateEmail();
      });
      emailInput.addEventListener('input', () => {
        if (fieldTouched.email) validateEmail();
      });
    }

    // Phone: live format with libphonenumber-js AsYouType
    if (phoneInput) {
      phoneInput.addEventListener('input', () => {
        const cursorAtEnd =
          phoneInput.selectionStart === phoneInput.value.length;
        const formatted = formatPhone(phoneInput.value);
        if (formatted !== phoneInput.value) {
          phoneInput.value = formatted;
          if (cursorAtEnd) {
            phoneInput.setSelectionRange(formatted.length, formatted.length);
          }
        }
        if (fieldTouched.phone) validatePhone();
      });
      phoneInput.addEventListener('blur', () => {
        fieldTouched.phone = true;
        validatePhone();
      });
    }

    // Interest: validate on change
    if (interestFieldset) {
      interestFieldset.querySelectorAll('input[name="interest"]').forEach((input) => {
        input.addEventListener('change', () => {
          fieldTouched.interest = true;
          validateInterest();
        });
      });
    }

    form.addEventListener('submit', async (e) => {
      e.preventDefault();

      fieldTouched.name = true;
      fieldTouched.email = true;
      fieldTouched.phone = true;
      fieldTouched.interest = true;

      const nameOk = validateName();
      const emailOk = validateEmail();
      const phoneOk = validatePhone();
      const interestOk = validateInterest();

      if (!nameOk || !emailOk || !phoneOk || !interestOk) {
        const firstError = form.querySelector<HTMLElement>(
          '[data-error-for]:not(.hidden)'
        );
        firstError?.scrollIntoView({ behavior: 'smooth', block: 'center' });
        return;
      }

      const submitButton = form.querySelector('button[type="submit"]') as HTMLButtonElement;
      const statusContainer = document.getElementById(`${formId}-status`);
      const successMessage = statusContainer?.querySelector('.success-message');
      const errorMessage = statusContainer?.querySelector('.error-message');

      if (submitButton) {
        submitButton.disabled = true;
        submitButton.textContent = 'Submitting...';
      }

      successMessage?.classList.add('hidden');
      errorMessage?.classList.add('hidden');
      statusContainer?.classList.add('hidden');

      const formData = new FormData(form);

      const nameValue = String(formData.get('name') || '').trim();
      const emailValue = String(formData.get('email') || '').trim().toLowerCase();
      const rawPhone = String(formData.get('phone') || '').trim();
      const phoneCheck = validatePhoneValue(rawPhone);
      const interests = getCheckedInterests();

      const data: any = {
        name: nameValue,
        email: emailValue,
        phone: rawPhone || undefined,
        phone_e164: phoneCheck.e164 || undefined,
        company: String(formData.get('company') || '').trim() || undefined,
        message: String(formData.get('message') || '').trim() || undefined,
        interests: interests.length > 0 ? interests : undefined,
        // Back-compat: also send first interest as scalar `interest`
        interest: interests[0] || undefined,
        source,
      };

      const utmParams = getUTMParams();
      if (utmParams.source) data.utm_source = utmParams.source;
      if (utmParams.medium) data.utm_medium = utmParams.medium;
      if (utmParams.campaign) data.utm_campaign = utmParams.campaign;
      if (utmParams.term) data.utm_term = utmParams.term;
      if (utmParams.content) data.utm_content = utmParams.content;

      try {
        const response = await fetch('/api/contact', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(data),
        });

        const result = await response.json();

        if (result.success) {
          trackFormSubmit(formId);

          if (statusContainer && successMessage) {
            statusContainer.classList.remove('hidden');
            successMessage.classList.remove('hidden');
          }

          form.reset();
          Object.keys(fieldTouched).forEach((k) => (fieldTouched[k] = false));
          form.querySelectorAll<HTMLElement>('[data-error-for]').forEach((el) => el.classList.add('hidden'));
          form
            .querySelectorAll<HTMLElement>('[data-validate]')
            .forEach((el) => {
              el.classList.remove('border-red-500', 'focus:ring-red-500');
              el.classList.add('border-gray-300');
            });
        } else {
          throw new Error(result.error || 'Submission failed');
        }
      } catch (error) {
        console.error('Form submission error:', error);
        if (statusContainer && errorMessage) {
          statusContainer.classList.remove('hidden');
          errorMessage.classList.remove('hidden');
        }
      } finally {
        if (submitButton) {
          submitButton.disabled = false;
          submitButton.textContent = submitButton.getAttribute('data-original-text') || 'Submit';
        }
      }
    });

    const submitButton = form.querySelector('button[type="submit"]');
    if (submitButton) {
      submitButton.setAttribute('data-original-text', submitButton.textContent || 'Submit');
    }
  });
</script>

<style>
  .contact-form-container {
    width: 100%;
    max-width: 100%;
  }
</style>
